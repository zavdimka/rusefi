defaultTasks 'shadowJar'

shadowJar {
    archiveFileName = 'gcc_map_reader.jar'
    destinationDirectory = file("$projectDir/..")

    manifest {
        attributes(
            'Main-Class': 'rusefi.GccMapReader'
        )
    }
}